# AIVA Clinic Dashboard v9 Site-ready

Эта версия подготовлена для сайта:

- Vercel — хостинг сайта
- Supabase Auth — вход по email/паролю
- Supabase Database — сохранение данных
- Supabase RLS — роли и ограничения доступа
- Supabase Storage — будущая база знаний с файлами

## Файлы

- `index.html` — сайт
- `style.css` — дизайн
- `app.js` — логика
- `config.js` — ключи Supabase
- `supabase-schema.sql` — SQL для Supabase
- `assets/` — лого

## Быстрый запуск локально без Supabase

Открой `index.html`. Если в `config.js` стоит `ENABLED: false`, дашборд работает локально как v8.

## Подключение Supabase

1. Supabase → SQL Editor → New query.
2. Вставить весь код из `supabase-schema.sql`.
3. Нажать Run.
4. Supabase → Authentication → Users → Add user.
5. Создать пользователя для себя.
6. Supabase → Table Editor → profiles → поставить себе роль `owner`.
7. Supabase → Integrations → Data API → скопировать API URL.
8. Supabase → Project Settings → API Keys → скопировать Publishable/anon key.
9. Открыть `config.js`.
10. Поставить:

```js
window.AIVA_CONFIG = {
  ENABLED: true,
  SUPABASE_URL: "https://project.supabase.co",
  SUPABASE_ANON_KEY: "your_key"
};
```

## Загрузка на Vercel

1. Создать GitHub репозиторий.
2. Загрузить все файлы.
3. Vercel → Add New → Project.
4. Выбрать репозиторий.
5. Deploy.

## Важно

В v9 для быстрого запуска данные дашборда сохраняются в таблице `dashboard_state` как JSON.
Это нормально для MVP. Когда структура окончательно утвердится, можно перейти на нормализованные таблицы:
`monthly_plans`, `daily_metrics`, `payments`, `knowledge_docs`.


## v9.2 fix
Если `dashboard_state.data` в Supabase пустой `{}`, дашборд автоматически создаёт стартовую структуру и сохраняет её в Supabase.


## v9.3 privacy update

- Чистая прибыль скрыта для всех ролей, кроме `approver`.
- ROMI тоже скрыт для всех ролей, кроме `approver`, потому что по нему можно косвенно посчитать прибыль.
- Для Владимира в `profiles.role` нужно поставить `approver`.


## v9.4 update

- В разделе "Планы" добавлены кнопки:
  - + Направление
  - + Менеджер
  - + Статья
- Новые планы сохраняются в Supabase.
- Кастомные направления подтягиваются в фильтр и маркетинговые таблицы.


## v9.5 privacy update

- План расходов скрыт для всех, кроме ролей `approver` и `finance`.
- Раздел "Финансы" скрыт для остальных ролей.
- В РНП строка "Финансы расходы" не показывает суммы и проценты тем, у кого нет финансового доступа.
- Для Владимира поставь `role = approver`.
- Для других лиц с доступом к расходам поставь `role = finance`.


## v9.6 full access update

- `owner` видит и редактирует всё, включая чистую прибыль, ROMI, финансы и план расходов.
- `approver` тоже видит и редактирует всё. Это роль для Владимира.
- Для обновления RLS в Supabase можно запустить `v9-6-role-update.sql`.


## v9.8 update

- Планы теперь можно полноценно редактировать и добавлять.
- Врач может быть привязан к нескольким направлениям.
- Ввод по врачам теперь считается по связке "врач + направление".
- Клиника автоматически суммирует врачей по направлениям.
- Сотрудников можно добавлять в панели; роль и строка доступа сохраняются.
- Для боевого входа сотрудника всё равно нужно создать в Supabase Authentication → Users.
- Роль `sales` редактирует только свою строку менеджера, если в поле "Доступ к строке / врачу" указать его имя.
- Роль `clinic` редактирует только своего врача, если указать ФИО врача в поле доступа.


## v9.9 update

- Раздел "Продажи" теперь берёт список менеджеров из "Планы → План отдела продаж".
- Переименование менеджера в плане автоматически переносит его ежедневные данные в продажах.
- Удаление менеджера из плана удаляет его строку из ежедневного ввода продаж.
- Добавление менеджера в план добавляет его в ежедневный ввод продаж.


## v10 access fix

- Доступы из раздела "Сотрудники" теперь имеют приоритет над ролью `viewer`, которая подтягивается из Supabase.
- РОПу ставь роль `manager`: он увидит планы, маркетинг, продажи, клинику, врачей, сотрудников и настройки, но без финансов/чистой прибыли.
- В списке сотрудников теперь можно редактировать имя, email, роль и доступ к строке/врачу.
- Если сотрудник уже создан в Supabase Auth, изменение роли в панели пробует обновить `profiles`.
- Для входа сотрудника на сайт его всё равно нужно создать в Supabase Authentication → Users.


## v10.1 privacy update

- KPI "Расходы" скрыт для всех, кроме `owner` и `approver`.
- Строка финансовых расходов в РНП также скрыта для всех, кроме полного доступа.
- Роль `finance` больше не видит расходы, если ей отдельно не дадут полный доступ через `owner`/`approver`.


## v11 role model

Новая модель ролей:

- `general_director` — Генеральный директор. Полный доступ ко всему. Для Владимира.
- `marketing_director` — Директор по маркетингу. Доступ ко всему, кроме финансов, расходов, чистой прибыли и ROMI.
- `sales_head` — РОП. Доступ к плану ОП и продажам. Без финансов/прибыли/расходов.
- `center_coordinator` — Координатор центра. Доступ к клинике, врачам и внесению расходов в финансах. Итоги расходов/прибыль скрыты.
- `clinic_director` — Директор клиники. Доступ только к финансам для внесения расходов. Итоги расходов/прибыль скрыты.
- `sales` — обычный менеджер продаж, редактирует только свою строку.
- `marketing` — маркетолог.
- `clinic` — клиника/врачебный блок.
- `viewer` — только просмотр.

Перед назначением новых ролей в Supabase запусти `v11-role-update.sql`.


## v11.1 custom metrics

- В маркетинге и продажах можно добавлять, удалять и переименовывать показатели.
- У каждого показателя можно включить:
  - `₸` — денежный показатель;
  - `РНП` — показывать/считать в РНП.
- РНП автоматически подтягивает все показатели, у которых включена галочка `РНП`.
- Новые показатели сохраняются в Supabase через общий dashboard_state.


## v11.3 safe rollback/fix

Эта версия собрана от стабильной v11.1.
Она не меняет структуру маркетинга/продаж и не должна обнулять отображение таблиц.
Исправлено только:
- ответственное лицо по маркетингу в РНП может быть `РОМ`;
- сохранена старая стабильная логика таблиц.


## v11.4 safe configurable doctors/plans

- В таблице врачей можно добавлять, удалять и переименовывать показатели.
- В планах по направлениям можно добавлять, удалять и переименовывать плановые столбцы.
- Для планового столбца можно выбрать источник факта: `marketing`, `clinic` или `none`.
- РНП автоматически строит строки по настраиваемым плановым столбцам.
- Версия собрана от safe-ветки без резкой миграции маркетинга/продаж.


## v11.5 city split

- Добавлен переключатель города: `Астана` / `Алматы`.
- У каждого города отдельное состояние dashboard_state:
  - `main_astana`
  - `main_almaty`
- Показатели, планы, врачи, продажи, маркетинг и финансы не дублируются между городами.
- При переключении города дашборд сохраняет текущий город и загружает отдельные данные выбранного города.


## v11.6 city + revenue update

- Добавлен выбор города: `Астана` / `Алматы`.
- Каждый город хранится отдельно:
  - `main_astana`
  - `main_almaty`
- Показатели городов не дублируются и не смешиваются.
- KPI `Выручка` теперь показывает общую выручку: `ОП + клиника`.
- В РНП добавлены отдельные строки:
  - `ОП выручка`;
  - `Клиника выручка`;
  - `Итого выручка`.
- Чистая прибыль считается от общей выручки: `ОП + клиника - расходы`.


## v11.7 revenue section

- Добавлен новый раздел в левом меню: `Выручка`.
- В разделе можно добавлять/удалять/переименовывать строки выручки.
- Суммы заполняются по дням, итог считается автоматически.
- Итог из раздела `Выручка` уходит в РНП как `Клиника выручка`.
- Выручка из таблицы врачей остаётся для аналитики врачей, но больше не используется как основная выручка РНП.
- KPI `Выручка` = `ОП выручка + раздел Выручка`.
